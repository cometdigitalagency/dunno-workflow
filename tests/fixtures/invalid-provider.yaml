project:
  name: invalid-provider-test
  repo: test/repo

tickets:
  source: file
  file:
    directory: ./tickets
    format: markdown

events:
  task:
    description: Work assignment
    format: "TASK: {description}"
    direction: lead -> worker

agents:
  lead:
    role: Lead
    description: Interactive lead agent
    interactive: true
    provider: claude
    model: sonnet
    tools: [read, bash]
    owns: { paths: [], never_edit: ["**/*"] }
    stack: []
    sends: [task]
    receives: []
    workflow:
      - "Take user input"
    rules:
      - "Do not write code"
    init:
      default: "I'm the lead."
      test: "{default}"
      issue: "{default}"
    prompt_extra: ""

  worker:
    role: Worker
    description: Worker with bad provider
    interactive: false
    provider: openai
    model: gpt-4
    tools: [read, edit, write, bash]
    owns: { paths: ["src/**"], never_edit: [] }
    stack: []
    sends: []
    receives: [task]
    workflow:
      - "Wait for TASK message"
    rules:
      - "Follow existing patterns"
    init:
      default: "Wait for tasks."
      test: "{default}"
      issue: "{default}"
    prompt_extra: ""

layout:
  mode: auto
  debug_pane: false
